angular.module('loginApp' []).controller('LoginController', function() {
    var login = this;
    login.username = {text:'Username'}
    login.password = {text:'password'}
    login.type = {text:''}
}